''' Python 3 Program - Print Floyd's Triangle '''

# Prompt number of lines
lines = int(input("How many lines to print? ")) + 1

k = 1  # Number that will be iterated through for printing
outString = ""
for i in range(1, lines):  # Iterates through lines
    for j in range(1, i + 1):  # Iterates through values in line
        outString += str(k) + " "
        k = k + 1
    print(outString)
    outString = ""
